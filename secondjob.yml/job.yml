name: myfirst workflow

on: push
job:
  first_job
  runs-on: ubuntu-latest
  steps:
   name: Welcome
   run: echo "My Github Action Job"

   name: List files
   run: ls

   name: Read file
   run: cat README.md